var sum = 10 + 20; // 변수에는 10 +20이 할당되어 생성된 숫자 값인 30이 할당됨

10 // 리터럴 표현식
sum // 식별자 표현식(선언 이미 존재할 때)
10 + 20 // 연산자 표현식
square() // 함수 호출 표현식(선언이 이미 존재 할 때)

var x; //변수 선언문
x  = 5; //할당문
function foo(){} //함수선언문
if( x>1 ){
    console.log(x); //조건문
}
for (var  i = 0; i <2; i++){
    consolel.log(x);    //반복문
}